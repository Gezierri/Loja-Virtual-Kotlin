<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gezierri.app_lojavirtual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppLojaVirtual">
        <activity android:name=".fragments.ProdutoCadastroActivity"></activity>
        <activity
            android:name=".activities.TelaPrincipalActivity"
            android:label="@string/title_activity_tela_principal"
            android:theme="@style/Theme.AppLojaVirtual.NoActionBar" />
        <activity android:name=".activities.FormCadatroActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.SlidesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>